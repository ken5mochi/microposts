<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <%= simple_format(h(micropost.content)) %>
    <%= image_tag micropost.photo.url, width: 300 if micropost.photo? %>
  </span>
  <span class="timestamp">

    <% if  micropost.bookmarked_by?(current_user) %>
      ❤️ <%= micropost.bookmarks.count %>
      <%= form_for(micropost.bookmarks.find_by(user_id: current_user.id),
        html: { method: :delete } ) do |f| %>
        <%= f.submit "remove from bookmarks", class: "btn btn-xs" %>
        <!--<button type="button" class="btn btn-default">❤️</button>-->
      <% end %>
    <% else %>
      ♡ <%= micropost.bookmarks.count %>
      <% unless micropost.user == current_user %>
        <%= form_for(micropost.bookmarks.build) do |f| %>
          <div><%= hidden_field_tag :user_id, current_user.id %></div>
          <div><%= hidden_field_tag :micropost_id, micropost.id %></div>
          <%= f.submit "add to bookmarks", class: "btn btn-primary btn-xs" %>
        <% end %>
      <% end %>
    <% end %>
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user == micropost.user %>
      <%= link_to "delete", micropost, method: :delete,
        data: { confirm: "Are you sure?" } %>
    <% end %>
  </span>
</li>